{
  "name": "assignment-2-testing",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^4.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
